# Per-agent LLM policy.
#
# Extraction stages stay deterministic/conservative.
# Reasoning is enabled only where semantic planning or verification
# benefits from it.

parser:
  thinking: false
  temperature: 0.0
  max_tokens: 8192
  json_mode: true

evidence_builder:
  thinking: false
  temperature: 0.0
  max_tokens: 12000
  json_mode: true

visual_interpreter:
  thinking: false
  temperature: 0.0
  max_tokens: 8192
  json_mode: true

narrative_planner:
  thinking: true
  temperature: 0.2
  max_tokens: 12000
  json_mode: true

claim_writer:
  thinking: true
  temperature: 0.2
  max_tokens: 12000
  json_mode: true

claim_verifier:
  thinking: true
  temperature: 0.0
  max_tokens: 12000
  json_mode: true

curator:
  thinking: false
  temperature: 0.1
  max_tokens: 8192
  json_mode: true

layout_optimizer:
  thinking: false
  temperature: 0.1
  max_tokens: 8192
  json_mode: true

color_agent:
  thinking: false
  temperature: 0.3
  max_tokens: 8192
  json_mode: true

font_agent:
  thinking: false
  temperature: 0.3
  max_tokens: 8192
  json_mode: true

section_title_designer:
  thinking: false
  temperature: 0.2
  max_tokens: 8192
  json_mode: true
